import React from 'react'

type Props = {
    pagedResult: PagedResult<PagedProduct>;
};

const ProductCard = ({pagedResult}: Props) => {
    return (
        <div>ProductCard</div>
    );
};

export default ProductCard;